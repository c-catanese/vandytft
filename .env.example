# PostgreSQL Database Configuration (Neon.tech)
PGHOST='your-neon-host.aws.neon.tech'
PGDATABASE='your_database_name'
PGUSER='your_database_user'
PGPASSWORD='your_database_password'
ENDPOINT_ID='your-endpoint-id'

# Riot Games API Key
# Get your API key from https://developer.riotgames.com/
RIOT_KEY='RGAPI-your-riot-api-key-here'

# Rate Limiting Configuration (optional - defaults provided)
# Number of users to process in parallel during bulk rank updates
# Lower values = slower but safer, higher values = faster but more API usage
# BATCH_SIZE=10
